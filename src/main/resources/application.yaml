spring:
  application:
    name: clients-api

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/clients_db}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
